{
  "name": "letter-box",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,desktop -c blue,magenta \"npm run dev -w @letter-box/server\" \"npm run dev -w @letter-box/desktop\"",
    "infra:up": "docker compose up -d postgres redis",
    "server:up": "docker compose up -d --build server",
    "server:down": "docker compose down",
    "infra:down": "docker compose down",
    "db:import:sqlite": "npm run db:import:sqlite -w @letter-box/server --",
    "build": "npm run build -w @letter-box/server && npm run build -w @letter-box/desktop",
    "test": "npm run test -w @letter-box/server",
    "typecheck": "npm run typecheck -w @letter-box/server && npm run typecheck -w @letter-box/desktop",
    "dist:mac": "npm run build && npm run dist:mac -w @letter-box/desktop"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=22"
  }
}
